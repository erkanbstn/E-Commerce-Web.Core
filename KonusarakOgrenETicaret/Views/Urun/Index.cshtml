@model List<RolTasiyici>
@{
    ViewData["Title"] = "Index";
}

@for (int i = 0; i < Model.Count; i++)
{
    @if (Model[i].RolAktif == true)
    {
        <div>Rolünüz : @Model[i].RolAd</div>
    }
}
@await Component.InvokeAsync("UrunListe") 